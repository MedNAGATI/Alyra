function palindrome(chaine) {
    for (( let i == 0 ; i < math.floor (chaine.length/);
    console.log(i, chaine[i]) Any 

    return true
    else
    Si la première et la dernière lettre sont identique, c’est un palindrome
    si le reste est un palindrome
